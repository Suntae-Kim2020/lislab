apiVersion: run.googleapis.com/v1
kind: Job
metadata:
  name: fix-categories
spec:
  template:
    spec:
      template:
        spec:
          containers:
          - image: gcr.io/hybrid-flame-323510/lislab-backend:latest
            args:
            - python
            - manage.py
            - fix_categories
            env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  key: latest
                  name: DATABASE_URL
